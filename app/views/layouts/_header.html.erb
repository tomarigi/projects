<nav class="navbar navbar-expand-md bg-secondary navbar-dark">
  <div class="container">
    <%= link_to 'Tobitate Portfolio β', root_path, :class => 'navbar-brand' %>
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"> <span class="navbar-toggler-icon"></span> </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
        <li class="nav-item"><%= link_to 'プロジェクト', user_projects_path, :class => 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'プロフィール', user_profile_index_path, :class => 'nav-link' %></li>
            <% if current_user.admin? %>
                <li class="nav-item"><%= link_to 'ユーザー管理', admin_user_index_path, :class => 'nav-link' %></li>
            <% end %>
        <li class="nav-item"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, :class => 'nav-link' %></li>
        <% else %>
        <li class="nav-item"><%= link_to 'ログイン', new_user_session_path, :class => 'nav-link text-white' %></li>
        <% end %>
      </ul>
      <form class="form-inline m-0">
        <input class="form-control mr-2" type="text" placeholder="Search">
        <button class="btn btn-primary" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>